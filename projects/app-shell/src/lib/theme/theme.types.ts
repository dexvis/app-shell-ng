/**
 * Theme mode supported by the shell.
 *
 * - `light` — force light color scheme
 * - `dark`  — force dark color scheme
 * - `auto`  — follow the user's system preference (prefers-color-scheme)
 */
export type ThemeMode = 'light' | 'dark' | 'auto';
